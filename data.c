#include <NDS/jtypes.h>

// These are dummy variables to allow the project to link.
// The final application will not work correctly without the
// real data from the original project files (likely a .s or .o file).

u16 font[1] = {0};
u16 fontpal[1] = {0};

u8 firmware[1] = {0};
u8 firmware_lite[1] = {0};